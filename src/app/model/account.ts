export class Account {
    firebaseID: string;
    name: string;
    email: string;
    phone: number;
    secretCode: number;     // 藍芽的密碼
    role: string;           // 權限
    parent: string;
}

